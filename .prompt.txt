Goal: simple proof run.

Return ONLY valid JSON with this shape:
{
  "changes": [
    { "path": "README.md", "op": "append", "text": "Auto PR test: it works.\n" }
  ],
  "title": "Auto PR: test append",
  "body": "Appended a single line to README.md"
}
