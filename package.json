{
  "name": "sdl-shopify-importer",
  "version": "1.1.0",
  "description": "SDL — Paste product links (Amazon, Luna, generic) → scrape (Zyte primary, ChatGPT parsing backup) → per-item L/W/H + margin → compute retail rounded to $5 → export Shopify Product CSV.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "build": "node -e \"console.log('Build check: OK')\""
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.57.0",
    "papaparse": "^5.4.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
